import 'package:flutter/material.dart';

class SubtasksPage extends StatelessWidget {
  const SubtasksPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Subtasks Page'));
  }
}
